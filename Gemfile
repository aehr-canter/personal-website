source "https://rubygems.org"

# github-pages bundles the exact Jekyll version + plugin versions that
# GitHub Pages runs in production, so what you see locally matches
# what gets deployed.
gem "github-pages", group: :jekyll_plugins

# Ruby 3+ removed webrick from the standard library; jekyll serve needs it.
gem "webrick", "~> 1.8"
